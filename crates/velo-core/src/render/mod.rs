// The render module contains the shared layout format (layout.json).
// CLI widget rendering (tiny-skia) lives in the separate `widgets-builtin` crate.
// GUI widget rendering (Canvas 2D) lives in `packages/widgets-builtin` (TypeScript).

pub mod layout;
